#include "main.h"

/**
 * *_strcpy - A funtion that copies the string pointed to by srcc 
 * including terminating null byte[\0] to the buffer pointed to by dest
 * @dest: Return value (Pointer to)
 * @src: pointer from
 * Return: dest (A program runs a success)
 */

char *_strcpy(char *dest, char *src)
{
	int a;
	int b;

	while 
