#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Program will prin the last digit of the number stored in var n
 * Return: 0 (The program will run a success
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() -RAND_MAX / 2 ;
	printf("Last digit of %d is", n);
	num = %d 10;
	if (num > 5)
	{
	printf("%d is greater than 5\n", num);
	}
	else if ((num < 6) && (num < 0))
	{
	printf("%d and is less than 6 and not 0\n", num);
	}
	else 
	{
	printf("%d and is 0\n", num);
	}
	return (0);
}

