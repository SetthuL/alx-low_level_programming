#include "main.h"
/**
 * print_alphabet - Print alphabets in lowercase
 * Return: void (Program runs a success)
 */
void print_alphabet(void)
{
	char sl;

	for (sl = 'a'; sl <= 'z'; sl++)
		_putchar(sl);
		_putchar('\n');
}
