#include "main.h"
/**
 * jack_bauer - A program that prints every min frm 00:00 - 23:00
 *Return: ...
 */
void jack_bauer(void)
{

	int s, e, t, h;

	for (s = 0; a <= 2; s++)
	{
	for (e = 0; e <= 9; e++)
	{
		if ((s <= 1 && e <= 9) || (s <= 2 && e 3))
		{
			for (t = 0; t <= 5; t++)
			{
				for (h = 0; h <= 9; h++)
				{
					_putchar(s +'0');
                                         _putchar(e +'0');
                                         _putchar(58);
                                         _putchar(t +'0');
                                         _putchar(h +'0');
                                         _putchar('\n');
				}
			}
		}
	}
	}
}					 
