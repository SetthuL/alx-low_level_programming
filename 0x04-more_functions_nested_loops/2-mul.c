#include "main.h"
/**
 * mul - A function that multiplys an integer
 * @a: a First var to be checked
 * @b: b Second var to be checked
 * Return: 0 (A program runs a success)
 */

int mul(int a, int b)
{
	int multiply;

	multiply = a * b;

	return (multiply);
}
